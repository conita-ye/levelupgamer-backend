-- data.sql de inicialización mínima para evitar error 'script must not be null or empty' en tests
-- Se puede extender con inserts reales según el esquema. Para PostgreSQL dejamos una sentencia válida.
SELECT 1;

